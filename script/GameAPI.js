import {Main} from './index7.0.js'  

export default class GameAPI {
    
    /**
     * Initialize private attributes 
     * 
     */ 
    #oPlaneGame = document.getElementByID("planeGame");             //the game board
    #bgArr = ["bg1.jpg","bg2.jpg","bg3.jpg","bg4.jpg","bg5.jpg"];   // background images 
    #oMyPlane = document.getElementById("myPlane");                 //the user's plane
    #oRelAlert = document.getElementById("relAlert");               //the prompt when game over
    #oScoreLTPlay = document.getElementById("scoreLTPlay");         //the score displayed at the top left corner
    #oScoreIn = document.getElementById("scoreIn");                 //the score to display when game over
    #oStop = document.getElementById("stop");                       //(never used )the "stop" menu to display





}

